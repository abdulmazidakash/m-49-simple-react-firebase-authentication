/**
 * 1. first install firebase: npm install firebase
 * 2. console.firebase.google.com create a project
 * 3. Register project
 * 4. get firebase config and do not save config on the client side
 * 5. create auth where it will be added
 * 6. export app from the firebase init file 
 * 7. in the console authentication > started > enabled Google provider
 * 8. go to firebase docs
 * 9. create auth (firebase initialize auth instance)
 * 10. go to the login button. add event handler and create a provider (google provider)
 * 11. call the signInWithProvider (auth, googleProvider)
 * 12. after successfully getting data, you may set it to a state.
 */